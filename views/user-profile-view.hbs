{{> menu active="profile"}}

<section>
  <div class="columns">
    <div class="column is-half">
      <h2 class="title has-text-left">
        Welcome, {{user.firstName}}
      </h2>
    </div>
  </div>
</section>
<section class="section">
  <h1 class="title is-4">You may update your account details below:</h1>
  {{#if success}}
  <div class="notification is-success is-light">
    <button class="delete"></button>
    Your details have been successfully updated.
  </div>
  {{/if}}

  <form action="/profile/update" method="POST">
    <label class="label">Edit your name</label>
    <div class="field is-horizontal">
      <div class="field-body">
        <div class="field">
          <input class="input" type="text" placeholder="Enter first name" name="firstName" value="{{user.firstName}}">
        </div>
        <div class="field">
          <input class="input" type="text" placeholder="Enter last name" name="lastName" value="{{user.lastName}}">
        </div>
      </div>
    </div>
    <div class="field">
      <label class="label">Edit your email address</label> <input class="input" type="text" placeholder="Enter email" name="email"
        value="{{user.email}}">
    </div>
    <div class="field">
      <label class="label">Choose a new password</label> <input class="input" type="password"
        placeholder="Enter your new password" name="password">
    </div>
    <div class="field">
      <label class="label">Confirm your new password</label> <input class="input" type="password"
        placeholder="Re-enter your new password" name="passwordConfirmation">
    </div>
    <div class="field is-grouped">
      <button class="button is-link">Update</button>
    </div>
  </form>
  <br>
  <div class="field">
    <label class="label">Delete my account</label>
    <a class="button is-link is-danger" href="/profile/delete" onclick="return confirm('Are you sure you want to delete your account?');">Delete My Account</a>
  </div>
</section>